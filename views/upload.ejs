<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload | PhotoShare</title>
  <link href="/css/style.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
</head>

<body>

  <nav class="navbar">
    <div class="nav-left">
      <a href="/" class="navbar-brand">PhotoShare</a>
    </div>
    <div class="nav-links">
      <span class="nav-item">@<%= user.username %></span>
      <a href="/" class="nav-item">Feed</a>
      <a href="/logout" class="nav-item logout-link" title="Logout">
        <i class="bi bi-box-arrow-right" style="font-size: 1.2rem;"></i>
      </a>
    </div>
  </nav>

  <div class="container mt-5">
    <div class="card" style="max-width: 600px; margin: 0 auto; background: #fff;">
      <h2 class="text-center mb-3" style="color: var(--text-color);">Upload New Moment</h2>
      <p class="text-center" style="color: var(--text-muted); margin-bottom: 30px;">
        Share your latest capture with the community.
      </p>

      <form action="/upload" method="POST" enctype="multipart/form-data">

        <div class="upload-area">
          <label style="cursor: pointer; width: 100%; display: block;">
            <div style="font-size: 3rem; margin-bottom: 10px;">📸</div>
            <span style="font-weight: 600; color: var(--primary-color);">Choose a Photo</span>
            <p style="font-size: 0.85rem; color: var(--text-muted); margin-top: 5px;">JPEG or PNG supported</p>
            <input type="file" name="photo" accept="image/*" required style="display: none;"
              onchange="document.getElementById('file-name').innerText = this.files[0].name">
          </label>
          <div id="file-name" style="margin-top: 10px; font-weight: 500; color: var(--secondary-color);"></div>
        </div>

        <div class="form-group">
          <label class="form-label">Caption</label>
          <input name="caption" class="form-control" placeholder="Write a catchy caption..." required>
        </div>

        <button class="btn btn-primary w-100 mt-3">Post to Feed</button>
      </form>
    </div>
  </div>

</body>

</html>