<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>SnapVerse - Share Instantly</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background: linear-gradient(to right, #1f1c2c, #928dab);
      color: white;
      font-family: 'Segoe UI', sans-serif;
    }

    .hero {
      padding: 80px 20px;
      text-align: center;
    }

    .hero h1 {
      font-size: 3rem;
      font-weight: bold;
    }

    .hero p {
      font-size: 1.2rem;
      margin-top: 15px;
      color: #ddd;
    }

    .hero-buttons {
      margin-top: 30px;
    }

    .hero-buttons a {
      margin: 0 10px;
      padding: 12px 24px;
      border-radius: 8px;
      font-weight: bold;
      text-decoration: none;
    }

    .btn-start {
      background-color: #00c9a7;
      color: #1f1c2c;
    }

    .btn-login {
      border: 2px solid white;
      color: white;
    }

    .features {
      padding: 60px 20px;
      background: rgba(255,255,255,0.05);
    }

    .feature-box {
      background: rgba(255,255,255,0.08);
      padding: 25px;
      border-radius: 12px;
      margin-bottom: 20px;
    }

    .footer {
      padding: 20px;
      text-align: center;
      font-size: 0.9rem;
      color: #ccc;
      background: rgba(0,0,0,0.2);
    }
  </style>
</head>
<body>

  <div class="hero">
    <h1>SnapVerse</h1>
    <p>Instant photo sharing with a cloud-native twist. Built for creators, powered by Azure.</p>

    <div class="hero-buttons">
      <% if (!user) { %>
        <a href="/signup" class="btn-start">Get Started</a>
        <a href="/login" class="btn-login">Login</a>
      <% } else { %>
        <a href="/upload" class="btn-start">Upload Photo</a>
        <a href="/logout" class="btn-login">Logout</a>
      <% } %>
    </div>
  </div>

  <div class="features container">
    <div class="row">
      <div class="col-md-4">
        <div class="feature-box">
          <h4>Creator Feed</h4>
          <p>Upload your best shots and share them instantly with the SnapVerse community.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="feature-box">
          <h4>Live Engagement</h4>
          <p>Like and comment on photos in real time. Stay connected with fellow creators.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="feature-box">
          <h4>Azure Powered</h4>
          <p>Hosted on Microsoft Azure with scalable storage and secure deployment.</p>
        </div>
      </div>
    </div>
  </div>

  <div class="container mt-5">
    <h2 class="text-center">Live Feed</h2>
    <div class="row justify-content-center">
      <div class="col-md-8">
        <% images.forEach((image, index) => { %>
          <div class="card bg-dark text-white mb-4">
            <img src="<%= image.path %>" class="card-img-top" alt="SnapVerse post">
            <div class="card-body">
              <h5 class="card-title"><%= image.user %></h5>
              <p class="card-text"><%= image.caption %></p>

              <form action="/like/<%= index %>" method="POST" class="d-inline">
                <button type="submit" class="btn btn-outline-light btn-sm">❤️ Like (<%= image.likes %>)</button>
              </form>

              <form action="/comment/<%= index %>" method="POST" class="mt-2">
                <div class="input-group">
                  <input type="text" name="comment" class="form-control" placeholder="Add a comment..." required>
                  <button class="btn btn-success" type="submit">Post</button>
                </div>
              </form>

              <ul class="mt-3">
                <% image.comments.forEach(c => { %>
                  <li><strong><%= c.user %>:</strong> <%= c.text %></li>
                <% }) %>
              </ul>
            </div>
          </div>
        <% }) %>
      </div>
    </div>
  </div>

  <div class="footer">
    © 2026 SnapVerse — COM769 Coursework Project
  </div>

</body>
</html>
